#include<stdio.h>
int main()
{
    int l, b;
    printf("ENTER LENGTH:");
    scanf(" %d", &l);
    printf("ENTER BREADTH:");
    scanf(" %d", &b);
    printf("area of rectangle is: %d", l*b);

    return 0;
}